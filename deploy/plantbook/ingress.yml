apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
  - host: "plantbook.icu"
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: plantbook
              port:
                number: 8081